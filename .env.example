# 前端构建期开关（Vite）。复制本文件为 .env.local 按需改，.env.local 不进 git。
#
# VITE_SEAL_TOOLS=true 时，「签名」工具里额外显示 圆章 / 方章 / 骑缝章。
# 公章/企业印章法律风险高，只给项目所有者在本机开发用——
# 服务器上 build 不放这个文件，对应 UI 根本不渲染，线上只有个人手写签名。
VITE_SEAL_TOOLS=false
